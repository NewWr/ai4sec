# Progress Master

Active phase: `knowledge-spine.md`

## Current Status

- Daily paper recommendations now target five broader interests: medical image deep learning, CLIP prompt learning, SAM-family segmentation, DINO-style self-supervised vision, and CLIP model design/transfer.
- Refresh failures from external arXiv/API calls are reported per topic instead of failing the whole recommendation API.
- The full daily-recommendation knowledge-space redesign is implemented: local spaces separate main/daily source and analysis knowledge, daily ingest requires per-paper parse-mode confirmation, abstracts are expanded by default, `/knowledge-spaces` supports edit/move/copy/remove/resync, and daily items can be promoted to the main research spaces.
- Analysis Dify sync now supports multiple datasets per run; daily recommendation Dify sync no longer falls back to the main research dataset when daily-specific datasets are not configured.
- `/daily` now lists recommendations across all dates by default, ordered by fetched date descending with 20 items per page, and the backend auto-refreshes daily recommendations every day at 06:00 Asia/Shanghai by default.
- LLM gateway settings can now be updated and connection-tested from `/settings`; runtime configuration is persisted under the backend data directory and is used by model listing, runs, library Q&A, and LLM calls without editing `.env`.
- Logic Lens LLM calls split the method deep-dive into pipeline and formulas requests, and LLM requests no longer send `max_tokens`/`max_output_tokens`; upstream 502/504 gateway errors fail fast, and the running LLM reasoning effort is set to `medium`.
- `/knowledge-spaces` is now the unified knowledge-base center: it keeps local item management and adds per-space Dify dataset creation/binding, Dify document listing, and Markdown preview; the separate `/library` route remains available but is no longer shown in the top navigation.
- `/knowledge-spaces` now uses a viewport-bounded workspace to prevent empty over-scroll, and Dify document preview supports expanded full-page reading.
- Upload-page collection suggestions now bypass the Next.js rewrite proxy and connect directly to the backend, avoiding proxy disconnects during long MinerU/LLM classification requests.
- Knowledge-spine P0 backend invariant is tightened: manual and AI fact cards now resolve `source_quote` into `research_evidence_items`, write `research_evidence_cards`, and cannot become verified unless evidence-backed or explicitly overridden.
- Knowledge-spine P1 card generation now stops template value-field fallback, keeps incomplete candidates in draft review, injects research profiles where available, and routes AI question/idea candidates into `research_gaps`.
- Knowledge-spine P2-P4 foundation is now wired: verified cards generate synthesis cards, `/synthesis` exposes synthesis/conflict/gap review, `/library/ask` fuses graph sources before Dify, `/daily` uses local behavior/gap signals for recommendation feedback, and `/writing` supports snippet composition, comparison tables, Markdown/BibTeX/RIS/Obsidian/Zotero CSL exports.
- Logic Lens now persists section-scoped extraction contexts, and AI card generation prioritizes those contexts before falling back to whole-report excerpts.
- Knowledge-spine P2 relation discovery now has a deterministic verifier layer (`rule_verifier_v1`) that records positive/negative checks, counter-evidence, `verifier_version`, and verified/needs-more-evidence status before synthesis/gap review.
- Knowledge-spine P2 now has hybrid verification wiring: deterministic rule verification remains the offline default, optional LLM relation verification can be enabled via `RESEARCH_DISCOVERY_LLM_VERIFY_*`, and verified action-card creation/promotion triggers local relation rediscovery plus synthesis rebuild.
- Knowledge-spine final closure is green across targeted backend pytest (56 tests), targeted ruff, and frontend `npm run build`; the formal Docker instance is rebuilt/restarted on `http://localhost:3001` with backend `http://localhost:8001`, and the temporary frontend dev server on 3003 has been stopped.
- `/settings` LLM connection testing now works in the formal Docker deployment: backend traffic uses the host proxy, 3001 rewrites to the host-network backend, and the test path falls back across Responses-with-reasoning, plain Responses, and Chat Completions for OpenAI-compatible gateways.
- `start_ai4sec.sh` now starts the full vendored Dify stack before AI4Sec services, using the `postgresql,weaviate,collaboration` profiles and verifying Dify web/API plus the local Dify proxy dataset API.
- Added `方案/research-second-brain-hardening-plan.md`, summarizing the current second-brain closure assessment and the concrete hardening roadmap: local graph-only QA, evidence-chain propagation, semantic verification, Gap lifecycle, writing quality, behavior feedback, and health observability.
- Research-second-brain hardening P0 is now implemented: `/library/ask` supports Dify-independent local graph QA, `graph_only` requests, and automatic fallback to local cards/evidence/relations/gaps/snippets when Dify is disabled or fails.
- Research-second-brain hardening P1-P5 are now implemented in one pass: evidence trace reaches writing snippets and exports, relation edges persist comparability checks, gaps have a full lifecycle/history model, writing supports paragraph plans plus traceable/clean output, asset/QA events feed ranking and health metrics, and `/writing`, `/synthesis`, `/health` expose the upgraded state.
- Latest verification is green for targeted backend pytest (23-test P1-P5 set and 35-test compatibility set), targeted ruff, and frontend `npm run build`.
- Local paper-reading state has been reset for a fresh start: formal/dev app DBs and `ai4sec-dify-sync` state were backed up under `backups/reset-reading-state-20260613-175721`, runtime paper/upload/download payloads were cleared, and the formal deployment is healthy with empty papers/cards/discovery/health state while default knowledge spaces and daily topics auto-initialize.
- Synthesis/writing UI polish is deployed: `/synthesis` Gap cards now have a full-detail "放大查看" dialog, and `/writing` cross-paper comparison now uses a searchable paper selector plus "from selected cards" selection instead of requiring manual `paper_id` lookup; formal frontend `http://localhost:3001` has been refreshed and verified.
- Synthesis Gap detail view now separates card preview and full content: card tiles keep the shortened `description`, while the "放大查看" dialog reads `full_description` restored from the original source block; targeted backend pytest, ruff, frontend build, and formal Docker deployment checks are green.
- Frontend Chinese localization foundation is implemented: `frontend/src/lib/labels.ts` centralizes enum/status labels, the default locale is now Chinese, and `/writing`, `/synthesis`, `/knowledge`, `/knowledge-spaces`, `/paper/[paperId]/run/[runId]`, `/papers`, `/library`, and PDF loading errors now avoid direct user-facing backend enum English where covered; frontend `npm run build` is green.
